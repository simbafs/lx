{
  "name": "lx-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "docs:dev": "pnpm --filter docs dev"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
